{% extends 'base.html.twig' %}

{% block body %}
    <h1>Delai Resultat</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ delaiResultat.id }}</td>
            </tr>
            <tr>
                <th>Valeurresultat</th>
                <td>{{ delaiResultat.valeurResultat }}</td>
            </tr>
            <tr>
                <th>Frequencerealisation</th>
                <td>{{ delaiResultat.frequenceRealisation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('delairesultat_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('delairesultat_edit', { 'id': delaiResultat.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
