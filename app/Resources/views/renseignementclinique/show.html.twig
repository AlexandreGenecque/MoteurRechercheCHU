{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="titre">RenseignementClinique</h1>

    <table id="table" class="table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ renseignementClinique.id }}</td>
            </tr>
            <tr>
                <th>Valeurrc</th>
                <td>{{ renseignementClinique.valeurRC }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="champsfiche">
        <li>
            <a href="{{ path('renseignementclinique_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('renseignementclinique_edit', { 'id': renseignementClinique.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-danger" type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
