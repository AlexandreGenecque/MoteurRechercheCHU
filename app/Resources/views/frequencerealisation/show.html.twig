{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="titre">FrequenceRealisation</h1>

    <table class="table-bordered table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frequenceRealisation.id }}</td>
            </tr>
            <tr>
                <th>Valeurfr</th>
                <td>{{ frequenceRealisation.valeurFR }}</td>
            </tr>
            <tr>
                <th>Precisionfr</th>
                <td>{{ frequenceRealisation.precisionFR }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="champsfiche">
        <li>
            <a href="{{ path('frequencerealisation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('frequencerealisation_edit', { 'id': frequenceRealisation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn-primary btn" type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
