{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="titre">Nature Matrice</h1>

    <table id="table" class="table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ natureMatrice.id }}</td>
            </tr>
            <tr>
                <th>Valeur matrice</th>
                <td>{{ natureMatrice.valeurMatrice }}</td>
            </tr>
            <tr>
                <th>Precision matrice</th>
                <td>{{ natureMatrice.precisionMatrice }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="champsfiche">
        <li>
            <a href="{{ path('naturematrice_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('naturematrice_edit', { 'id': natureMatrice.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-danger" type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
