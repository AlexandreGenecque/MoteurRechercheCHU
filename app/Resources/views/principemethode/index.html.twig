{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste Principe Méthode</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom de la principe méthode</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for principeMethode in principeMethodes %}
            <tr>
                <td><a href="{{ path('principemethode_show', { 'id': principeMethode.id }) }}">{{ principeMethode.id }}</a></td>
                <td>{{ principeMethode.nomPrincipeMethode }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('principemethode_show', { 'id': principeMethode.id }) }}">Voir</a>
                        </li>
                        <li>
                            <a href="{{ path('principemethode_edit', { 'id': principeMethode.id }) }}">Modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('principemethode_new') }}">Créer une nouvelle entrée</a>
        </li>
    </ul>
{% endblock %}
