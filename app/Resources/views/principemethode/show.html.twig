{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="titre">Principe Méthode</h1>

    <table id="table" class="table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ principeMethode.id }}</td>
            </tr>
            <tr>
                <th>Nom de la Principe Méthode</th>
                <td>{{ principeMethode.nomPrincipeMethode }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="champsfiche">
        <li>
            <a href="{{ path('principemethode_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('principemethode_edit', { 'id': principeMethode.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-danger" type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
