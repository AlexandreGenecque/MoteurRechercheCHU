{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer Conservation Avant Transport</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('conservationavanttransport_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
