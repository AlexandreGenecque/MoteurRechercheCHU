{% extends 'base.html.twig' %}

{% block body %}
    <h1>Conservation Avant Transport</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conservationAvantTransport.id }}</td>
            </tr>
            <tr>
                <th>Valeurconservation</th>
                <td>{{ conservationAvantTransport.valeurConservation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('conservationavanttransport_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('conservationavanttransport_edit', { 'id': conservationAvantTransport.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
