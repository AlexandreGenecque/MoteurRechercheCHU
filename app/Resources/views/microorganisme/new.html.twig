{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer MicroOrganisme</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('microorganisme_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
